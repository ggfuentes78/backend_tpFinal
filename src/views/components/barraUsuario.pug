div(class="container bg-secondary mt-3")
    h1(class="alert bg-secondary") Bienvenido, #{user.email}!
        form(action="/api/auth/logout" method="post" autocomplete="off")
            button(class="btn btn-danger float-end mt-2") Desloguear
        form(action=`/api/carrito/${user.carrito}` method="get")
            button(class="btn btn-success float-end mt-2") Ver Carrito
        form(action=`/api/auth/${user._id}` method="get")
            button(class="btn btn-primary float-end mt-2") Perfil